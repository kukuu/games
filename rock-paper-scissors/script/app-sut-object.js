//Using objects
module.exports = function(user, comp){
	let defeaters = {
		r: 'p',
		p: 's',
		s: 'r'
	};

	//If user is defeater of comp(computer)
	if(user === defeater[b]){
		return 'user';
	}

	//If comp is defeater of user
	if(comp === defeater[a]){
		return 'comp';
	}

	return 'draw';
}